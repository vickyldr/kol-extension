# 按钮逻辑清单（v0.27 改版施工图）

> 这是「先理逻辑、再改代码」的施工图。每个按钮写清：**在哪 / 点了干嘛 / 数据哪来 / 调不调 AI / 结果显示哪**。
> 标记：🆕 新增　✏️ 改　❌ 删　✅ 不变
> **审阅方式**：对的打勾，错的直接在那条下面写「改成 XX」。全部定稿后才动代码。

---

## 总览：三个面（这次只改前端画面 + 采集逻辑）

| 面 | 是什么 | 跑在哪 |
|---|---|---|
| **独立待办窗口** | 开工自动弹，今天要干嘛清单 | `reminders.html`（弹出窗） |
| **sidepanel 回复页** | 常驻右侧，回复单个红人 | `sidepanel.html` 主区 |
| **后台采集**（看不见） | 开对话自动采集 → 最新进度 | `content.js` + `kol-reminder.js` |

---

## 一、顶部常驻栏（sidepanel 最上面）

| 按钮 | 标记 | 点了干嘛 | 数据 / AI | 结果显示 |
|---|---|---|---|---|
| ❓ 指引 | ✅ | 展开/收起新手指引 | 无 | guide-card |
| 🔔 提醒 | ✏️ | 在面板内快速瞄一眼提醒；**主推**旁边「🪟 弹独立窗口」 | 读 `kolThreads`/`kolTodos` | reminder-panel |
| 📚 话术库 | ✏️ | 打开话术库面板；**上传话术挪进这里面** | `/api/playbook`+`/api/knowledge` | archive-panel |
| 📎 物料库 | ✅ | 打开物料库 | `/api/assets` | assets-panel |
| ~~📥 上传话术~~ | ❌ | **删掉这个独立按钮**，功能移进「📚 话术库」面板内 | — | — |
| 状态灯 | ✅ | 点一下重新检测服务 | `/health` | 按钮文字 |

> ❓ 指引文案 ✏️：现在写的是「红人来消息/我主动发/问AI」三板块，**要重写**成新结构（回复页 + 待办窗口）。

---

## 二、⚙️ 服务器设置（折叠面板，填一次收起）✅ 基本不动

| 控件 | 标记 | 说明 |
|---|---|---|
| 服务地址 / 团队口令 / 管理员口令 / ins id / 产品 | ✅ | 第一次填好 → 保存并连接 → 收起 |
| 💾 保存并连接 | ✅ | 存设置 + 重测服务 + 同步提醒身份 |
| ☁️ 从云端恢复 / 💾 导出 / 📂 导入 | ✅ | 云备份 / 离线备份 |

> 没填 ins id / 产品时顶部橙色软提示横幅 ✅ 保留。

---

## 三、独立待办窗口 `reminders.html` ✏️ 重点改

**整体**：开工**自动弹出**（🆕）；窗口内实时刷新（已回就走、对方再发又回）。

**顶部**
| 控件 | 标记 | 说明 |
|---|---|---|
| 标题「🔔 今天要干嘛」 | ✏️ | — |
| 刷新 | ✅ | 手动重算 |
| 🆕 统计条 | 🆕 | 🔴X 立即回复 · 🟠X 今天跟进 · 📅X 以后 |

**三个桶（折叠）** 🆕
| 桶 | 默认 | 进桶条件 |
|---|---|---|
| 🔴 立即回复 | 展开 | `needsReplyRaw` 且未被「不用提醒」压制；在线排最前 |
| 🟠 今天跟进 | 折叠 | AI 判定 `needs_follow_up` 到期 + 今天到点的待办 |
| 📅 以后 | 折叠 | 未来到点的待办 |

**每条卡片**
| 元素 | 标记 | 内容 / 来源 |
|---|---|---|
| 头像 | ✅ | `thread.avatarUrl`，没有则首字母彩色圆 |
| 名字 | ✅ | `pickInboxTitle()` 识别 |
| 🆕 **一行总结** | 🆕 | **「目前进度 + 该干嘛」**，来自最新进度（红人最新消息 + 上次离开总结）。没采集过的兜底显示「最新消息预览 + 还没读过，点进去看一眼」 |
| 「已等 X 分钟 / 天」 | ✅ | 计时锚点 `lastCreatorMessageAt` |
| **打开对话** | ✅ | threadId 深链跳 IG；抓不到 ID 退回收件箱首页 |
| 不用提醒了 / 完成 / 删除 / 🔕 别再提醒 | ✅ | 压制该条 / 待办 done / dismiss / 静音群 |

---

## 四、sidepanel 回复页（顺序 = 你定的）

> ❌ **删掉顶部三 tab**（红人来消息 / 我主动发 / 问 AI）。回复页就一个，不再切换。
> ❌ **删掉「我主动发」「问AI」两个整块**及其全部 JS。
> ❌ **删掉「📋 合作进展存档」面板**（手动📸抓取这屏整套）。

### 4-1　🗂️ 红人档案（第 1 位，折叠）✏️ 上移到最顶
| 按钮 | 标记 | 点了干嘛 | 数据 |
|---|---|---|---|
| 自动对齐 | ✅ | 打开对话时自动匹配当前 IG 对话名 | `KOL_GET_CONVERSATION_TITLE` |
| 查 | ✅ | 按名字查档案 | `kolProfiles`（本地） |
| 💾 保存 / 🗑️ 删除 | ✅ | 存/删该红人档案（App ID/合同/付款/备注） | `kolProfiles` |

### 4-2　🧠 AI 理解（第 2 位）🆕 全新置顶块
| 元素 | 标记 | 说明 |
|---|---|---|
| 触发 | 🆕 | 打开对话后**自动跑**（不是粘贴触发）。数据 = 提醒的总结 + 当前停留界面对话内容，综合 |
| 默认展开 | 🆕 | 可折叠 |
| 紫色「禁止外发」徽标 | 🆕 | 红线1：内部建议绝不混入对外 |
| 当前阶段 / 建议下一步 / 发送前风险 | 🆕 | 来自 `/api/analyze` |
| ⧉ 复制 | 🆕 | 复制这份理解给自己看 |

### 4-3　⚡ 我的快捷回复（第 3 位，折叠）✅
| 按钮 | 标记 | 点了干嘛 | 数据 / AI |
|---|---|---|---|
| 搜索框 | ✅ | 打字本地秒匹配 | `kolQuickReplies`（本地，**不喂 AI**） |
| 点一条结果 | ✅ | 填进回复区 | — |
| ＋ 手动加 → 保存 | ✅ | 存个人快捷 | 本地 |

### 4-4　红人原文（第 4 位）✅
| 按钮 | 标记 | 点了干嘛 | AI 接口 | 结果显示 |
|---|---|---|---|---|
| 贴 / ✕ 清空 | ✅ | 粘贴原文 / 清空 | — | message 框 |
| ❓ 这是什么意思 | ✅ | 翻译 + 潜台词 | `/api/ask`(meaning) | ask-answer |
| 🤔 这怎么办 | ✅ | 结合对话给主意 | `/api/ask`(howto) | ask-answer |

### 4-5　我想回复（中文）（第 5 位）✅
| 按钮 | 标记 | 点了干嘛 | AI 接口 | 结果显示 |
|---|---|---|---|---|
| 🔤 忠实翻译 | ✅ | 一字不改照翻 | `/api/translate` 或 reply(faithful) | result 双语 |
| 📝 润色生成 | ✅ | AI 润色成自然回复 | `/api/reply` | result 双语 |
| 回复语言下拉 | ✅ | 指定输出语言（日语走敬语层） | — | — |
| 📖 从话术库选回复话术 | ✅ | 打开话术库弹窗挑 | `/api/playbook` | playbook-dialog |

### 4-6　✅ 双语回复（A·可发）（第 6 位）✅
| 按钮 | 标记 | 点了干嘛 | AI 接口 |
|---|---|---|---|
| 左右分屏 / 选句高亮 | ✅ | 每句对齐 | — |
| 复制全部外语 / 单句复制 | ✅ | 复制 | — |
| ✅ 按修改后的中文改写外语 | ✅ | 改中文后回改外语 | `/api/rewrite` |
| 让 AI 改 / 生成双语（改写框） | ✅ | 一句话指令改 | `/api/rewrite` |
| ⭐ 存为快捷 / 保存为话术 | ✅ | 搬进个人快捷 / 存团队话术 | 本地 / `/api/archive` |
| 识别&内部提醒（折叠在下） | ✏️ | 详细分析；**与顶部 AI 理解共用同一次 `/api/analyze`**，不重复调 | `/api/analyze` |
| 保存为确认话术 | ✅ | 存档 | `/api/archive` |

---

## 五、话术库 / 物料库 / 上传 ✏️

| 入口 | 标记 | 说明 |
|---|---|---|
| 📚 话术库面板 + 弹窗 | ✅ | 搜索合并 playbook+knowledge；每条「⭐ 存进快捷」 |
| 🆕 话术库内「📥 上传话术」 | 🆕 | 把原顶部按钮的上传功能移进来（选 .docx → 本地解析 → `/api/knowledge/import`，仅管理员） |
| 📎 物料库 | ✅ | 上传/查看物料 `/api/assets` |

---

## 六、后台采集（看不见，不占界面）🆕 改采集方式

| 环节 | 标记 | 逻辑 |
|---|---|---|
| 开对话即采集 | 🆕 | 打开对话起自动捕捉当前屏对话；AI 自定节奏自动逐屏，直到抓完整段 |
| 离开对话前总结 | 🆕 | 离开前把这次（画面对话 + 红人最新消息）总结成「最新进度」，存 `kolSummaries` |
| ❌ 手动一屏一屏追加 | ❌ | 删掉旧的 grab() 手动拼接 |
| 喂给两处 | 🆕 | 最新进度 → ① 提醒每条总结　② 回复页 AI 理解的底料 |

---

## 七、删除清单（一次性确认）

- ❌ 顶部三 tab：红人来消息 / 我主动发 / 问 AI
- ❌ 整块「我主动发」`panel-proactive`（free-intent、模板生成、result-pro 等）
- ❌ 整块「问 AI」`panel-chat`
- ❌ 「📋 合作进展存档」面板 + 手动📸抓取这屏
- ❌ 顶部「📥 上传话术」独立按钮（移进话术库）
- ❌ 相关 JS：`generateFree` `rewriteGoPro` `translateProReply` `sendChat` `renderChat` `loadQuickTemplates` `generateQuickTemplate` `selectQuickTemplate` 等

---

## 待你确认的几处（拿不准的，先问你）

1. 🟠 今天跟进的「到期天数」用 AI 给的 `follow_up_after_days`，没有就默认 2 天 —— 行吗？
2. AI 理解自动跑：每次打开对话都跑一次（费 token）vs 有缓存先显示、变化才重算 —— 你倾向哪个？
3. 独立待办窗口「自动弹出」：每次开浏览器都弹 vs 只有 🔴 有内容才弹 —— 选哪个？
4. version 升到 `0.27.0` 对吗？
