# KOL 双语沟通助手 · 使用指南

> 一句话：帮你**看懂红人外语消息、生成双语回复、出主意、整理合作进展、提醒别漏回红人**。
> 装一次就行，话术和 AI 更新全自动，你什么都不用做。

---

## 一、这是什么 / 能干嘛

- 自动把红人发来的外语消息翻成中文
- 一键生成「中英双语」回复，复制就能发
- 回复不满意，让 AI 接着改到满意
- 主动发消息、砍价、催稿、催发布都有现成话术
- 自动整理每个红人的合作进展（报价/合同/初稿/付款……），一眼看出走到哪
- 提醒你谁的消息还没回、谁答应给东西还没给
- 像 ChatGPT 一样随时问 AI（翻译、思路、流程都行）
- logo、教程图、参考视频、链接，全团队一个地方共享

支持 **Chrome / Edge 浏览器**，用于网页版 **Instagram / Gmail / Outlook**。

---

## 二、安装

找管理员要：**团队口令** + 商店链接。

### 如果你之前是解压安装的

先把旧的迁移到商店版（以后再也不用手动更新）：

1. 插件侧边栏 → 「**⚙️ 服务器设置**」→「**💾 导出备份**」（把你的设置存下来）。
2. Chrome 地址栏输入 `chrome://extensions` → 找到「KOL 双语沟通助手」→ 点「**移除**」。
3. 打开管理员发的商店链接 → 「**添加至 Chrome**」。
4. 进「⚙️ 服务器设置」→「**📂 导入恢复**」把备份导回来，填团队口令 → 保存并连接。

### 如果你是第一次用

1. 打开管理员发的**商店链接** → 点「**添加至 Chrome**」→「**添加扩展程序**」。
2. 浏览器右上角点 🧩 拼图图标 → 找到「KOL 双语沟通助手」→ 点图钉 📌，钉到工具栏。
3. 完成。以后插件**自动更新**，什么都不用做。

---

## 三、第一次设置（一次性，全在「⚙️ 服务器设置」里）

点工具栏插件图标，右边弹出侧边栏，进「**⚙️ 服务器设置**」一次填完：

- **服务地址**：默认填好了，不用改。
- **团队口令**：填管理员给你的口令（注意大小写）。
- **管理员口令**：普通成员**留空**。
- **你的名字（员工名）**：填了管理后台才知道「这红人是谁对接的」。
- **你的 ins id**：你自己的 Instagram 账号。它同时是提醒里「我自己的号」，**也是云端备份的钥匙**——填了之后你的进度/提醒/快捷/红人档案会自动存到云端，换电脑、清缓存都不丢（见第八节）。
- **你负责的产品**：选你对接的产品，生成话术默认用它。
- 点「**保存并连接**」，右上角显示「**千问已连接**」就对了。

> 没填 ins id / 产品时，界面顶部会出一条橙色提示，按它去补上即可（不影响先用）。

✅ 装好后：Instagram 私信里红人的外语消息，下方会自动出现中文翻译。

---

## 四、和红人沟通（最常用）

跳进一个红人对话后，回复页从上到下就是一条线，照着点：

### 🧠 AI 理解（最上面，自动跑）
打开对话**自动出**，给你三句参考：**当前阶段 / 建议下一步 / 发送前风险**。
- 不用点，秒级出（有缓存先显示，对话有新消息才重算，省时省钱）。
- ⚠️ 这块是**给你自己看的参考，别整段发给红人**。

### ⚡ 我的快捷回复
打几个字搜你自己存的快捷话术 → 点一下直接填进回复框，秒发。

### 红人发来的原文（外语）框
看不懂红人那句话 → 贴进这个框：
- 点「**❓ 这是什么意思**」→ AI 翻成中文、讲清言外之意。
- 点「**🤔 这怎么办**」→ AI 结合对话给你出主意（拿不准怎么处理时用）。

### 我想回复（中文）框
想回红人 → 在这个框写中文，然后二选一：
- 「**🔤 忠实翻译**」：照你写的原话翻，不增不减。
- 「**📝 润色生成**」：让 AI 润色得更自然，生成双语回复。
- 先写清意图（如`委婉拒绝` / `砍到250`）再生成，会更准。

### ✅ 双语回复（出结果的地方，可发）
- **左右分屏逐句对照**（左外语 | 右中文），自动对齐。
- 满意就点「**复制全部外语**」发给红人；点单句「复制」只拿那一句。
- 想自己改意思：**只改右边中文格**（外语那列只读），再点「**✅ 按修改后的中文改写外语**」。

### ✍️ AI 改写
回复不满意 → 在「写一句话让 AI 改」里写「太长精简 / 语气更热情 / 别提价格」→ 点「**让 AI 改 / 生成双语**」，反复改到满意。

### 📖 从话术库选回复话术
要现成话术时点它，从话术库挑、填变量、复制发出去。

> **AI 会自动读你当前打开的 IG 对话当参考**，🧠 AI 理解和生成回复都用得上，不用额外粘贴。

---

## 五、📋 当前合作情况（再也不怕忘了聊到哪一步）

**解决什么烦恼？** 红人一多，谁发合同了？谁付款了？谁的初稿还没来？每次都要翻半天聊天记录。

**你要做什么？——基本啥都不用做。** 正常和红人聊天，插件在后台自动整理成一张进展清单：

```
✅ 报价确认 — 300USD 已谈妥
✅ 要到账号 — 已给 IG handle
⬜ 合同已签 — 未见
⬜ 初稿 — 在等
✅ 付款 — 已转
⚠️ 备注：红人最近想延期，留意
```

一眼知道走到哪、还差啥。

**全自动，基本不用你操作**：你打开红人对话时插件就在后台采集，离开这个对话前自动整理出「最新进度」。你只管正常聊天，它自己整理。

---

## 六、🔔 提醒（别漏回红人、别忘了催）

**解决什么烦恼？** 谁发消息没回、谁答应给东西结果没给、你催了对方不理，太容易漏。

**提醒长在一个独立小窗口里，每天打开 Chrome 会自动弹出来**，像 todo 清单告诉你今天该处理谁。里面分几个桶：

- 🔴 **立即回复**：红人发来消息你还没回（含没点开过的未读 + 点开了没回的）。你回了它就消。
- 🟠 **今天跟进**：今天到点（含已过期）的待办，到点提醒你去催账号/合同/初稿。
- 📅 **以后**：你自己定了未来日期的待办，时间没到先放这。
- 📤 **该催对方**：**你发了消息红人却不回** → 会逐级提醒你「该催了」：二次跟进 → 再跟进 → 最后通牒 → 建议终止。
  - 点这条的「**✍️ 生成话术**」，AI 直接出一句催场话术（日语会用委婉的「見送り」措辞，不伤和气）。
  - **发不发你自己定**，AI 只帮你把话准备好。

每条卡片都有一句中文总结（红人说到哪、该干嘛），点「**打开对话**」直接跳到那个红人。

**顺手小功能：**
- 🪄 **智能加待办**：直接写「5天后小树枝交稿」「明天下午3点催 Anna」，自动帮你定好时间。**开着某个红人对话时加待办，会自动关联到这个红人**，之后清单里就能直接「打开对话」。
- **会话里的小提示**：跟红人聊合作但**还没约交稿时间**时，IG 输入框上方会冒一条黄提示，点一下就能插一句话去问。

---

## 七、📚 话术库 与 📎 物料库

**话术库**：全团队共享。可搜索、点「+ 新增」手动加、点「导入」批量加。（编辑/删除已有话术需要管理员口令。）

**物料库**：存 logo、教程图、参考视频、链接，按产品分类。
- 图片/视频 → 「下载」存到本地再发。
- 链接 → 「复制链接」直接粘进 DM。
- 新增 → 「+ 上传物料」，所有人都能加。
- 大视频建议存成「🔗 链接」（云盘/YouTube），比上传文件快。

---

## 八、更新说明

| 更新内容 | 你要做什么 |
|---|---|
| 话术库、AI 效果 | ✅ 自动，无需操作 |
| 插件界面/新功能（商店版） | ✅ 自动，Chrome 静默更新 |
| 插件界面/新功能（手动压缩包版） | 管理员发新包 → 解压覆盖原文件夹 → `chrome://extensions` 点 🔄 刷新 |
| 你的数据（进度/提醒/快捷/红人档案） | ✅ 填了 ins id 就自动存云端；换电脑填同样的 ins id → 启动自动恢复，或「⚙️ 服务器设置」点「☁️ 从云端恢复」。也可继续用「💾 导出 / 📂 导入」做离线备份 |

---

## 九、关于安全

- 只读你自己打开的页面，**不登录你的账号、不替你发消息**。
- 需要 AI 处理时，只把那几句对话文本发到**团队自己的服务器**。

---

## 十、常见问题

| 现象 | 怎么办 |
|---|---|
| 右上角「待配置 / 口令不正确」 | ⚙️ 服务器设置 → 确认团队口令填对 → 保存并连接 |
| 红人消息没自动翻译 | 确认「千问已连接」，刷新一下 Instagram 页面 |
| 翻译/生成失败 | 稍等重试；一直不行找管理员 |
| 紫色内部建议能发给红人吗 | ❌ 绝对不能，那是给你自己看的 |
| 桌面通知不弹 | Windows：设置→系统→通知→打开 Chrome；Mac：系统设置→通知→Chrome 允许 |
| 关了浏览器要重新填口令吗 | 不用，填一次永久保存 |
| 以前是解压包版，想换商店版 | ⚙️ 服务器设置 → 导出备份 → 移除旧插件 → 装商店版 → 导入备份 |

---

### 附：管理员维护（仅管理员）

- VPS 仓库在 `/home/ubuntu/kol-repo/`。后端是 **systemd 服务 `kol-assistant`**（别再手动 `nohup node server.js`，会撞端口）。更新服务端三步：
  ```bash
  cd /home/ubuntu/kol-repo && git pull origin main
  sudo systemctl restart kol-assistant
  curl -s localhost:3210/health        # 看到 ok:true 就成功
  ```
- **红人资源库 / 进度看板是另一个服务 `kol-roster`（端口 3220）**，更新它用：
  ```bash
  sudo systemctl restart kol-roster
  curl -s localhost:3220/health
  ```
- 更换 API Key / 团队口令：`cd /home/ubuntu/kol-repo && bash vps/setup.sh`
- 话术库备份：插件「话术库 → 导出备份」，或 VPS 的 `~/kol-data/` 目录。
- 编辑/删除话术、删除物料：在「⚙️ 服务器设置」填入**管理员口令**后即可操作。
