# KOL 双语沟通助手 · 功能说明书（实时维护）

> 这份文档逐个功能讲清楚：**输入什么 → 输出什么 → 解决什么烦恼 → 背后给 AI 问的是什么 → 设计思路 → 和相邻功能的区别**。
> 以后想改某个功能、或要跟别人解释「这个按钮干嘛的」，看这一份就够。
> 维护方式：每次改动功能，顺手更新对应小节。当前对应版本：**v0.24.9**。

> ⚠️ **2026-06 大改版进行中（开发分支 `claude/sleepy-fermat-eqatjn`，未合 main、未发布）。**
> 下面有几节描述的是**改版前**的界面，已过时；新界面以 **`按钮逻辑清单.md`**（逐按钮施工图）+ `CLAUDE.md §0b` 为准。过时小节已标 ~~删除线~~ 或 🔁 提示。
> 主要变化：① 回复页砍成一条线、删三 tab；② 新增 🧠AI理解（开对话自动跑）；③ 提醒改独立窗口三桶、每条一行**中文**总结；④ 删合作进展存档/导出导入/识别&内部提醒等。合并发布后再回头把各节正文重写对齐。

---

## 0. 总原则（安全红线）

- **只读不写**：插件只「读」Instagram 网页上已经渲染出来的文字，**绝不调用 IG 接口、绝不替你自动发消息、绝不动你账号**。所有「发送」都是你自己手动点 IG 的发送键。
- **数据存本地**：合作进度、提醒、待办都存在你浏览器的 `chrome.storage.local` 里，不上传服务器。只有「翻译/分析/总结」这类要 AI 算的，才把那段文字发到我们自己的服务器转给千问。
- **扩展 ID 已固定**（v0.22.1 起）：更新插件不会丢本地数据。

---

## 1. 顶部 · 通用设置

### 1.1 ⚙️ 服务器设置（唯一配置入口 · v0.24.7 收口）
- **输入**：服务地址（默认已填好 VPS；本机调试改 `http://127.0.0.1:3210`）、团队口令、**你的 Instagram ID**、**你负责的产品**、管理员口令（选填）。
- **输出**：右上角变「已连接」；ins id + 产品填好后橙色提示横幅消失。
- **解决**：一个地方填完就能用——口令让 AI 功能可用；ins id 让提醒认得「你自己的号」+ 充当云端备份的钥匙；产品让 AI 带上对应卖点和「禁止乱说」限制。
- **思路**：所有配置收到这一处。ins id / 产品保存时同步写进 `kolReminderSettings`（`syncReminderIdentity()`），不再有单独的「身份设置」表单。没填会出软提示横幅但**不锁按钮**。普通成员填地址+口令+ins id+产品即可；管理员口令才能编辑/删除话术、上传团队库。
- **区别**：产品选择以前在主框，现已移进这里；提醒里的「身份设置」已删除（与 ins id 重复）。

### ~~1.3 💾 导出备份 / 📂 导入恢复~~（🔁 改版已删，云端备份替代）
> 改版后移除导出/导入文件按钮——云端自动备份（按 ins id）已够用。以下为旧描述存档：
- **输入**：点导出生成一个 JSON 文件；点导入选一个之前导出的 JSON。
- **输出**：导出＝一个备份文件；导入＝把合作进度/提醒/待办/身份设置合并恢复。
- **解决**：换电脑搬家、或万一数据出意外时还原。
- **思路**：按对话 key 合并，不会用空数据覆盖已有的。

### 1.4 📥 团队库（上传话术 Word 更新知识库 · 仅管理员）
- **输入**：顶部「📥 团队库」→ 选一份话术 **`.docx`**（Word）。知识库太长、带大小标题/表格/图片，没法粘贴，所以走文件上传。
- **怎么解析**：插件在**你电脑本地**就把 Word 拆开（零依赖，用浏览器自带的解压 + XML 解析，不给服务器加任何包）：
  - 按**标题层级**认出「产品（VivaVideo / VivaCut / Rythmix…）→ 语种/地区（英语欧美 / 台湾 / 阿语…）」；
  - 每张**表格的每一行** = 一条话术，字段按表头（步骤 / IF对方问题 / THEN我方回答 / 中文 / 各语种）；
  - 表格里嵌的**示例截图**连同所在「产品/语种/场景」一起带出。
- **预览**：先显示「X 条话术 / Y 张表 / Z 张图，识别到哪些产品和语种」，确认无误再点导入。
- **入库（服务器侧）**——为「在旧文档上补充/修改后重导」这个真实工作流设计：
  - **完全重复**（逐字一样）→ 自动**跳过**；
  - **能对上库里已有的某条**（同 `stable_id` / 同「产品|语种|场景」/ 正文高度相似）但内容不一致 → 交 **AI 逐条判**这是「**改了旧条**」还是「**另起的新话术**」：
    采用新版替换旧条(take_new) / 保留旧版(keep_old) / 合并(merge) / 当成新条两条都留(new)。没配 AI 时默认采用新版。
    > 关键：匹配**按场景/内容**而不只看行号——你在旧文档里插行/挪动导致位置变了，改过的那条仍会被认成「对旧条的修改」，而不是平白多一条。
  - **完全对不上**库里任何条 → **新增**；
  - **示例截图**进**物料库**（按内容哈希去重，重复导入不会堆积）；
  - 导入前**自动备份**旧库到 `knowledge-base.backup-时间戳.json`，导错可回滚。
  - 匹配索引只用「原有库」，不在一次导入内部互相合并——同一份 Word 里每行都是作者有意安排的独立条目。
- **输出**：一张回执——新增 / 改旧条 / 保留旧版 / 重复跳过 / 入库图片数（含 AI 对每条冲突的处理理由）/ 备份文件名。
- **边界**：写入**只进线上目录**（VPS `~/kol-data/`），**绝不回写代码种子**，不污染仓库（见 §6）。
- **权限**：需要管理员口令（普通成员能预览解析结果，但点确认会被服务器拒绝）。

---

## 2. 主区 · 跟红人沟通（一列）

> 两个输入框，分工明确：
> **第一框「红人发来的原文（外语）」**＝看不懂的、对方发来的话。
> **第二框「我想回复（中文）」**＝你想说的中文 / 你的疑问。

### 2.1 🔤 忠实翻译
- **输入**：第二框写中文（你的成稿）。第一框有红人原文时，用来判断翻成哪国语言。
- **输出**：上方「双语回复」卡——左外语 / 右中文，逐句对照。
- **给 AI 问的**：「把这段中文**一字不改、不润色、不加客套**地翻成目标语言。」
- **解决**：你已经想好怎么说，只要精准翻过去，不要 AI 自由发挥。
- **思路**：温度=0，强调保留主语/否定/数字/语气。目标语言＝你手选的「回复语言」；没选就跟随红人原文语言；红人原文也空就退回你设的「常用语言」。
- **区别**：和「润色生成」的区别＝忠实=照翻，润色=会帮你写得更自然。

### 2.2 📝 润色生成
- **输入**：第二框写中文（成稿**或**只是大概意思，例如「委婉拒绝涨价，给两点理由」）。
- **输出**：同上，双语回复卡。
- **给 AI 问的**：「判断这是成稿还是写作意图，结合红人原话、上下文、产品资料，生成自然、专业、像真人的正式回复。」
- **解决**：你只有个大概想法，让 AI 帮你组织成得体的外语回复。
- **思路**：温度=0.25，允许润色但禁止编造价格/日期/授权/链接。
- **区别**：比忠实翻译「聪明」，但也因此可能加东西——要原样照翻就用忠实翻译。

### 2.3 ❓ 这是什么意思（看懂·含潜台词）
- **输入**：可填可不填。**不填也能用**——它会自动扫描当前对话框。
- **输出**：「🤖 AI 回答」区——大白话逐句讲清在说什么 + **可能的言外之意/潜台词**。
- **给 AI 问的**：`/api/ask`，**永远先用扫描工具读当前对话框**（通常最全），再结合你粘贴的原文；明确告诉 AI「**以信息更全的一方为准**」——通常扫描的更全，但你粘贴里若有扫描没有的内容，就两边结合。
- **解决**：看不懂、或拿不准对方话里藏着什么意思；也防止你粘贴的原文其实不全自己却没察觉。
- **思路**：要「聪明、会多说几句」，所以结合完整上下文解读，而不是干巴巴翻译。
- **区别**：翻**别人发来的、还要解读** → 这个；验**AI 给你的回复** → 看 2.6 的自动逐句对照。

### 2.4 🤔 这怎么办
- **输入**：第二框写你的疑问（例如「这个人死活不同意降价怎么办」）。第一框红人原文会一起带上。
- **输出**：「🤖 AI 回答」区——给思路/建议，**不生成给红人的回复**。
- **给 AI 问的**：`/api/ask`，把你的问题 + 当前对话（自动抓） + 红人原文 + 产品资料一起给 AI 出主意。
- **解决**：拿不准怎么处理，想要个参谋；或想问跟回复无关的问题。
- **思路**：自动读你停在的那段 IG 对话当背景，不用你手动粘。
- **区别**：「这是什么意思」=看懂字面；「这怎么办」=出主意、给策略。

### 2.4b ⚡ 我的快捷回复（个人 · 本地 · 打字秒出）
- **输入**：在搜索框打几个字（触发词或内容里的词）。
- **输出**：即时蹦出你存的快捷条目，点一条 → 直接填进双语回复卡。
- **怎么存**：① 生成回复后点卡片上「⭐ 存为快捷」，填个触发词即可；② 或「＋ 手动加一条」自己写。
- **解决**：每个人都有自己高频用的几十条话术，不该每次去翻团队大库；打几个字秒出最省事。
- **思路**：**纯本地关键词匹配，不走 AI、不走服务器** → 准、即时、离线也能用。存在浏览器 `kolQuickReplies`，跟着「💾导出备份」一起备份。
- **区别**：团队话术库＝共享、权威、配置在服务器；这个＝个人、私有、存本地、求快。

### 2.5 📖 从话术库选回复话术（团队共享库）
- **输入**：点开，搜/选一条已存好的话术。
- **输出**：套进双语回复卡。
- **解决**：高频场景不用每次重写，直接调现成话术。

### 2.6 双语回复卡（结果区 · 自动逐句对照）
- **左右分屏，每句自动对齐**：左外语 | 右中文，**每次生成回复都自动逐句对齐**（后台 `/api/align`，保证一句外语对一句中文），**不用按任何按钮**。
  - 先用按标点的快速切分瞬间显示，随后自动替换成对齐可信的版本。
  - **鼠标移到一行** → 两边对应那句一起高亮，一眼知道哪句对哪句。
  - **点左边外语** → 把整行标记住（持久高亮），方便回头挑句。
  - **点「复制」**（每行右侧，悬停出现）→ 只复制这一句外语。
- **改在中文这边**（你不懂外语，外语只读）：
  - **右边中文格可以直接改**（你自己改意思）；
  - 改完点 **「✅ 按修改后的中文改写外语」** → AI **重新改写**外语来匹配你的中文（是改写润色、不是死板直译），再自动逐句对齐。
- **复制全部外语**：一键复制整条，去 IG 粘贴发送。
- **保存为话术**：把这条存进话术库复用。
- **回复语言**：默认「自动（跟随红人语言）」，也可手选英语/韩语/泰语等。
- **或：写一句话让 AI 改**：不想自己改中文、只想给个指令时用，写「太长精简点 / 语气热情点 / 别提价格」→「让 AI 改」。
- **解决**：① 不确定 AI 翻得对不对 → 对照一眼看穿哪句翻歪；② 回复有两三句、你只想用其中一句 → 知道哪句外语对应哪句中文，单独复制那一句。

### ~~2.7 识别 & 内部提醒（折叠在下方 · 后台加载）~~（🔁 改版已删，由置顶「🧠 AI 理解」取代）
> 改版后这块整块删除，`/api/analyze` 改喂顶部「🧠 AI 理解」（打开对话自动跑，输出阶段/下一步/风险）。以下为旧描述存档：
- **触发**：第一框有红人原文时，点忠实翻译/润色生成后自动后台分析并展开。
- **输出**：识别语言、对话阶段、对方意图、可能的言外之意、备选回复、风险提示、**要不要先问 TL**、等待期可先发的临时回复、对方提到的物料、还缺哪些信息。
- **给 AI 问的**：`/api/analyze`，对红人这条消息做深度理解。
- **解决**：新人吃不准对方话里的潜台词、不知道要不要请示 TL。
- **思路**：和回复分两段——回复 3-5 秒先出，分析慢几秒补上，不挡你干活。
- ⚠️ 紫色「内部处理建议」是给你自己看的，**别发给红人**。

---

## ~~3. 当前合作情况（进度清单）~~（🔁 改版：删手动📸抓取，改"开对话自动采集"）

> 改版后删掉「📥 抓取这屏」手动抓屏 + 合作进展存档面板。**进度总结改为后台自动**：打开对话即采集当前屏、离开对话前自动调 `/api/summary` 生成「最新进度」存 `kolSummaries`，喂给①提醒卡片一行总结 ②回复页 AI 理解底料。以下为旧描述存档：

- **输入**：点「📥 抓取这屏」把当前可见对话拉进来（可多次滚动累积），或自己粘贴；点「🔄 总结这段对话」。
- **输出**：一张一眼能扫的清单——✅已做 / ⬜没做 / ⚠️备注（合同发了没、积分冲了没、brief 发了没、初稿收了没……）。
- **给 AI 问的**：`/api/summary`，把对话整理成固定顺序的 12 步流程清单；带上已有进展做**增量更新**，不覆盖旧的。
- **解决**：聊到一半忘了走到哪、还差啥；过几天想不起来「到底给 Ta 发合同没」。
- **思路**：
  - 存在 `kolSummaries`，**按对话固定 ID 存**（v0.21.9 起），名字识别有出入也不丢匹配。
  - 你切到别的对话时**自动后台更新**这张清单。
  - 「✅此合作已完结」→ 把这个对话静音，不再提醒。
- **区别**：这是「整段关系的进度」；提醒是「该我做某件事的待办」。

---

## 4. 🔔 提醒　（🔁 改版：改成独立窗口三桶 + 每条一行中文总结）

> **改版后**：提醒主界面 = 独立窗口 `reminders.html`（开浏览器自动弹），三桶 🔴立即回复 / 🟠今天跟进 / 📅以后；侧边栏 🔔 面板简化成「加待办 + 打开完整清单按钮」。每条卡片有**一行中文总结**（真总结 ‖ AI 把预览归纳成中文 ‖ 留空），绝不贴外语原文、绝不只显示「未读」。详见 `按钮逻辑清单.md` 三、和 `CLAUDE.md §0b`。以下小节多为改版前描述，部分仍适用（判定阈值、深链兜底）：

> 三类提醒，全部**只读 DOM 判断**，不碰 IG，不替你回。

### 4.1 身份设置（认我方 vs 红人）
- **现在在哪填**：已并进「⚙️ 服务器设置」——你的 ins id 当 handle、你负责的产品当前缀（v0.24.7 起没有单独表单了）。
- **作用**：插件能区分「我方同事」（前缀开头）和「红人」。群里同事已回 ≠ 你已处理；只有**你自己**回了才消提醒。
- **思路**：蓝色/右对齐气泡＝「我」；前缀开头＝同事；其余＝红人。

### 4.1b「打开对话」深链（v0.24.9 修）
- **作用**：提醒卡上点「打开对话」直接跳到 IG 那条私信。
- **坑与修法**：IG 收件箱列表行这版不一定是链接，从列表扫出的提醒可能没存对话数字 ID（`threadId` 空），那时只能退回收件箱首页（看着像"打不开"）。现在 `scanInbox()` 会尽量从行内 `<a href="/direct/t/xxx/">` 抓 ID 存进记录；抓不到照旧退首页。**已躺在提醒里的旧记录需重新划到/打开一次那条对话才会补上深链。**

### 4.2 待回复 / 待跟进 / 待办
- **待回复**：红人发来了、你没回（收件箱「N 条新消息」/蓝点判断）。
- **待跟进**：口头答应了却没推进（要 id/合同/初稿等），AI 按话术流程节点判断。
- **待办**：你手动加的事，或 AI 自动建议的。
- **给 AI 问的**：`/api/judge`，按话术流程节点表判断该不该提醒、卡在哪一步。
- **输出**：插件图标小红点 + 桌面通知 + 清单。
- **思路**：闹钟 1 小时跑一次；寒暄不用回的会自动忽略，不瞎提醒。

### 4.3 智能加待办
- **输入**：打一句话，例如「5 天后小树枝交稿 / 明天下午 3 点催 Anna」。
- **输出**：自动解析成「事项 + 日期 + 时间」存进待办。
- **给 AI 问的**：`/api/parse-todo`，自然语言 → 结构化时间。
- **自动关联红人（v0.29.2）**：加待办时如果你**正开着某个红人对话**，自动把那条对话的 `threadId` 顺手绑进待办，这条待办在清单里就能「一键打开对话」。
  - **没开对话/没认出**：**不**显示打不开的「打开对话」按钮（避免误导），改在输入框下方出一行**引导**：填红人名字 → 在本地 `kolThreads`（采集层按名字存的对话表，含 `threadId`）里反查匹配，找到就补绑，找不到就提示「可能还没聊过/采集过」。匹配用与采集层一致的 `titleKey` 归一化 + 群名/key 互相包含 + 去空格容错（"小 美"↔"小美"）。

### 4.4 弹出清单窗口 / 启动弹窗
- **解决**：你懒得开侧边栏。开 Chrome 时若有当天该办的，自动弹一个 todo 小窗口。

---

## 5. 后台 · 服务端（给懂技术的人看）

### 5.1 接口一览
| 接口 | 干什么 | 用的模型 |
|---|---|---|
| `/api/reply` | 快出双语回复（3-5 秒） | 快（qwen-flash） |
| `/api/analyze` | 深度分析红人消息 | 快 |
| `/api/rewrite` | 翻译/润色/改写（faithful / chinese_to_target / refine / target_to_chinese） | 快 |
| `/api/translate` | 纯翻译 + 难词注释 | 快 |
| `/api/ask` | 问 AI / 看懂 / 出主意 | 快 |
| `/api/judge` | 提醒判断（该不该提醒、卡哪步） | 慢（qwen-plus/3.7） |
| `/api/summary` | 合作进度 12 步清单（增量更新） | 快 |
| `/api/parse-todo` | 自然语言 → 定时待办 | 快 |
| `/api/knowledge/import` | 团队库 Word 导入：合并 + 判重/AI 判冲突 + 图片入物料库（仅管理员） | 快（仅判冲突时调用） |

### 5.2 模型路由（哪个按钮用哪个模型 · 影响速度和花钱）

| 你点的按钮 / 功能 | 接口 | 模型 |
|---|---|---|
| 对话框实时翻译 | `/api/translate` | 🟢 flash（快） |
| 🔤 忠实翻译 / 📝 润色生成 / ✅按中文改写 | `/api/rewrite` | 🟢 flash |
| ❓这是什么意思 / 🤔这怎么办 | `/api/ask` | 🟢 flash |
| 快出双语回复 | `/api/reply` | 🟢 flash |
| 识别分析红人消息 | `/api/analyze` | 🟢 flash |
| 合作进度总结 | `/api/summary` | 🟢 flash |
| 智能加待办（自然语言→时间） | `/api/parse-todo` | 🟢 flash |
| 双语逐句对齐 | `/api/align` | 🟢 flash |
| **🔔 后台提醒判断（该不该提醒、卡哪步）** | `/api/judge` | 🔴 plus（慢但更准） |

- **原则**：用户面前要等结果的**全用快模型**（flash）追求速度；**只有后台默默判断提醒**用慢模型（plus，更准，反正没人等）。
- **省钱要点**：交互按钮本来就是 flash，挪不出空间；账单大头其实是**后台 judge 用 plus、每小时对每条对话都跑**。真要省，就把 `DASHSCOPE_MODEL_SMART` 改成 `qwen-flash`（VPS 环境变量，不用改代码），代价是提醒判断略糙。量不大时不必动。
- flash 和 plus 各自有**独立的免费额度**（一次性赠送、有有效期）；免费额度用完后，若没开「免费额度用完即停」，会**自动接着扣已充值的余额**，不会自动跳回免费。

### 5.3 缓存
- `translateCache`（24 小时）、`judgeCache`（1 小时）：同样的文字不重复花钱、更快。

---

## 6. 数据存放与边界（拆仓库 / 换电脑 / 分享前必看）

> 一句话：**代码在 GitHub，话术/物料/知识库（线上版）在 VPS 服务器，进度/提醒在各人浏览器。三条线互不相干。** 拆代码仓库只动第一条，后两条纹丝不动。

| 资料 | 存在哪 | 在 git 里吗 | 拆仓库 / 更新代码会动它吗 |
|---|---|---|---|
| 插件代码、文档 | GitHub 仓库 `bilingual-extension/` | ✅ 在 | 跟着代码走（应该的） |
| **出厂种子话术**：`data/playbook.json`、`knowledge-base.json`、`quick-templates.json` | 仓库 `data/` | ✅ 在 | 跟着代码走（这是"装上就带的默认话术"） |
| **团队在插件里新增的话术存档、物料库** | VPS `~/kol-data/`（`scenario-archive.json`、`assets/`） | ❌ 不在 | ❌ 不动 |
| **你单独维护 / 多项目的知识库、话术脚本（线上覆盖版）** | VPS `~/kol-data/`（同名文件覆盖种子） | ❌ 不在 | ❌ 不动，更新代码也不覆盖 |
| 产品资料 `products.json` | VPS `~/kol-data/` | ❌ 不在 | ❌ 不动 |
| 各人**合作进度 / 提醒 / 待办 / ⚡个人快捷回复** | 各自浏览器 `chrome.storage.local`（含 `kolQuickReplies`） | ❌ 不在 | ❌ 不动（连服务器都不在；可用「💾导出备份」搬家） |

**放哪里 = 看你要什么效果（这就是区分规则）：**

- 想让一份话术**「跟着代码走、所有人装插件都带上、随版本更新」** → 放仓库 `data/`（出厂种子）。
- 想让它**「线上随时改、团队共享、不进代码、更新代码时不被覆盖」** → 放 VPS `~/kol-data/`。

**服务器怎么自动区分（`seedOrLive`）**：读 `playbook/knowledge-base/quick-templates` 时，
**先看 `~/kol-data/` 里有没有同名文件**——有就用线上那份（你的覆盖版），没有才用代码种子。
所以你以后要更新其它项目 / 单独维护的知识库，**只放到 VPS `~/kol-data/`，永远不会污染代码仓库、也不会被代码更新冲掉**。改完在 VPS 重启一下服务即可生效。

**关于拆仓库**：拆出去的只是 `bilingual-extension/` 这份代码（含 `data/` 种子）。
团队天天用产生的话术、物料、进度全在上面那两条「不在 git」的线里，**一个都不会被挪走**。
插件打包的 zip 里**也不含 `server.js` 和 `data/`**——同事的浏览器插件不携带任何话术，运行时才从服务器取。

---

## 6b. 话术库合并团队库 + 云端自动备份（v0.24.6）

**话术库 = 话术脚本 + 团队库（合并搜）**
- 输入：界面「📚 话术库」搜索框。
- 输出：同时搜「预置话术脚本 `playbook.json`」**和**「团队库 Word 导入落库的 `knowledge-base.json`」。
- 解决什么：以前团队库 Word 上传的内容只有 AI 后台用得到，运营在话术库里**搜不到**；现在两者合并，上传即可搜可用。条目上有来源标签（`团队库` / `话术脚本`）。
- 思路：后端新增 `GET /api/knowledge` 返回团队库；前端 `loadPlaybook()` 拉两份合并，`knowledgeToPlaybook()` 把 `{scene, fields:{语言:文本}}` 归一成话术库条目结构。

**云端自动备份（按 ins id）**
- 输入：「⚙️ 服务器设置」里填**你的 Instagram ID**。
- 输出：合作进度 / 提醒 / 待办 / 个人快捷（`BACKUP_KEYS`）在改动后**自动**悄悄存到服务器 `~/kol-data/backups/<insid>.json`；换电脑 / 清缓存后，填同一个 ins id → 启动时自动静默恢复，或点「☁️ 从云端恢复」。
- 解决什么：数据原本只在浏览器本地，清缓存 / 换电脑就丢。
- 思路：后端 `/api/backup`（GET 按 id 取、POST 存，id 清洗成安全文件名、大小写归一）；前端监听 `chrome.storage.onChanged`，命中 `BACKUP_KEYS` 就防抖 2.5s 推一次。恢复时对象型按 key 合并、其余「本地为空才填」，不覆盖更新的本地数据。原有「💾导出/📂导入文件」离线备份保留，二选一。

## 6c. 设置收口 + 话术库/快捷分工（v0.24.7）

- **服务器设置 = 唯一配置入口**：地址（默认填好）+ 团队口令 + **你的 ins id** + **你负责的产品**。ins id 同时充当提醒里「我自己的号」、产品同时是「我负责的产品」（保存时写进 `kolReminderSettings`）。
- **删除**：主框「当前产品」选择条（移进设置）、提醒里的「⚙️ 身份设置」表单（与 ins id 重复、入口太深）。
- **软提示横幅**：没填 ins id / 产品时，顶部出现橙色提示条引导去设置（不锁死功能）。
- **话术库 vs 快捷回复 分工说清**：
  - 话术库（团队库，会**喂给 AI**）：弹窗顶部加说明；每条加「⭐ 存进快捷」一键搬到个人快捷；管理员「📥 上传话术」用 Word 批量增补。
  - 快捷回复（个人本地，**不喂 AI**、打字秒出）：放主路径。
  - 「📥 团队库」按钮更名「📥 上传话术」——团队库即话术库，不再是两个概念。

## 6d. 红人档案扩展字段 + 最近跟进时间戳（v0.28.0，给「红人资源库/进度看板」喂数据）

- **红人固定档案卡新增字段**（`sidepanel.html` / `initKolProfile`）：外号、红人类型（口播/街采/宝妈）、脚本/方向、主题、性格备注（脾气/会不会脱稿）、值得合作（勾选）+ 推荐理由、黑名单（勾选）+ 拉黑原因。
  - 输入：实习生在档案卡顺手填；输出：随 `kolProfiles` 自动云备份，供管理端两个新网页（`apps/roster/`）只读展示。
  - 解决什么：红人资源库要的「价格/主题/值不值得/黑名单」等信息，插件原来没采集——补上这层输入。
- **`lastFollowUpAt`（最近跟进时间戳）**：`kol-reminder.js` 在「待回复 → 已回复」翻转时（实习生刚回复）盖时间戳，随 `kolThreads` 备份。
  - 用途：进度看板算「多久没跟进 / 今日盯人有没有漏待回复」。只加字段，不动 `needsReplyRaw` 判定（守 §9.14）。
- **`kolUnderstanding` 进 `BACKUP_KEYS`**：把 AI 算的「当前阶段」同步上云，供进度看板显示当前阶段。
- 详见 `apps/roster/设计.md`（红人资源库 + 进度看板设计文档）。

## 7. 待定 / 讨论中

- **三个「翻译/验证」入口已厘清**（v0.22.4 定）：
  - 看对方发来的 + 解读潜台词 → 「❓这是什么意思」（结合上下文，聪明版）；
  - 验 AI 给你的回复 → 双语回复卡**自动逐句对照**（无需按钮，悬停高亮、单句可复制）；
  - 页面里逐条自动翻译 → 字面、被动、挂在消息旁。
- rythmix / aicatch / vivavideo 的产品卖点细节待补全。
