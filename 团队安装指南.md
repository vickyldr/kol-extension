# KOL 双语沟通助手 · 团队安装与更新指南

> 给团队成员看的。装一次就行，以后话术和 AI 更新都是自动的，你什么都不用做。

---

## 一、第一次安装（每人做一次，约 1 分钟）

### 方式 A：从 Chrome 商店安装（推荐）

1. 打开管理员发给你的商店链接，点「**添加至 Chrome**」。
2. 弹出确认框，点「**添加扩展程序**」。
3. 点浏览器工具栏的插件图标，打开右侧边栏，点顶部「**⚙️ 服务器设置**」：
   - 服务地址：**已经默认填好了**，无需修改。
   - 团队口令：填**管理员发给你的口令**。
   - 管理员口令：**留空**（普通成员不用填）。
   - 点「**保存并连接**」，右上角显示「**千问已连接**」就成功了。

完成！打开 Instagram 就能用了。

---

### 方式 B：手动安装压缩包（商店不可用时备用）

1. 拿到管理员发的插件压缩包，**解压**到一个固定文件夹（比如桌面，别留在压缩包里）。
2. 打开 Chrome，地址栏输入 `chrome://extensions` 回车。
3. 打开右上角的「**开发者模式**」。
4. 点「**加载已解压的扩展程序**」，选中刚才解压出来的文件夹（里面能直接看到 `manifest.json` 那一层）。
5. 同上，进「⚙️ 服务器设置」填团队口令 → 保存并连接。

---

## 二、以后怎么更新？

分两种，绝大多数时候你**什么都不用做**：

| 更新内容 | 你要做什么 |
|---|---|
| **话术库**（新增/修改话术） | ✅ 自动，无需操作（话术存在服务器，实时同步） |
| **AI 逻辑 / 翻译效果** | ✅ 自动，无需操作 |
| **插件界面 / 新功能**（商店版） | ✅ 自动，Chrome 会静默更新，无需操作 |
| **插件界面 / 新功能**（手动压缩包版） | 管理员发新压缩包 → 解压后**覆盖进原来那个文件夹** → `chrome://extensions` 点插件卡片上的 **🔄 刷新** |

> ⚠️ 手动版更新时**别**「下载到新位置 → 删旧的 → 加载新文件夹」，那样路径变了会丢本地记录。

---

## 三、从旧压缩包版迁移到商店版

以前是手动装的 zip 版？可以一次性换成商店版，以后再也不用手动更新：

1. Chrome → 工具栏插件图标 → 「⚙️ 服务器设置」→「💾 导出备份」（先把设置存下来）。
2. `chrome://extensions` → 找到「KOL 双语沟通助手」→「**移除**」。
3. 打开管理员发的商店链接 → 「**添加至 Chrome**」。
4. 进「⚙️ 服务器设置」→「📂 导入恢复」把刚才的备份导回来。
5. 填团队口令 → 保存并连接。完成。

---

## 四、常见问题

- **右上角显示「AI 未启动 / 待配置」？** 打开「⚙️ 服务器设置」确认团队口令填对了，点「保存并连接」。
- **看不到「选话术」里的话术？** 确认已「千问已连接」；换个产品或清空搜索再看。
- **关掉浏览器后还在吗？** 在，装一次就一直在，除非你手动移除。
