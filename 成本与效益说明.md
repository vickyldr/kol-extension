# KOL 双语沟通助手 · 成本与效益说明（给团队 / 领导）

> 一句话：**这套工具（插件 + 资源库/看板那张表）一个月的真金白银开销，大概在几百元人民币级别，前期还有免费额度可能接近 0；换来的是每月省下数百小时人力 + 防漏回防失礼防乱报价。投入产出在 10 倍以上。**
>
> 下面把"用什么模型、什么时候花钱、20 人用一个月大概多少、值不值"逐项讲清楚。所有口径都来自代码实际配置，不是拍脑袋；真实账单随时能在阿里云百炼后台核对。

---

## 一、我们用的是什么模型（关键：刻意选了最便宜的）

| | 用的模型 | 说明 |
|---|---|---|
| 用户面前所有功能 | **qwen-flash** | 阿里云百炼最便宜的一档，速度快 |
| 后台提醒判断 | **qwen-flash** | 本来用更贵的 qwen-plus，已**特意改成 flash 省钱**（线上 `MODEL_SMART=qwen-flash`） |

- **全线用 flash 是有意为之**：用户要等结果的功能，要的是"快 + 便宜"；后台判断也压到 flash，进一步省。
- 对比一下贵的：`qwen-plus` 输入价是 flash 的 **约 3–4 倍**。我们没用它。
- 实际跑的是哪个模型，打开后端 `/health` 三个字段（`model / model_fast / model_smart`）就是实时真相。

**计费单价（本文估算的依据，按偏高的保守值算，实际更低）：**

| 模型 | 输入 | 输出 |
|---|---|---|
| qwen-flash（我们用的） | 约 ¥0.0003 / 千 tokens（官方第一档低至 ¥0.0002） | 约 ¥0.002 / 千 tokens |
| qwen-plus（没用，仅对比） | ¥0.0008 / 千 tokens | ¥0.002 / 千 tokens |

> tokens ≈ 字数：1 千 tokens 粗略相当于 500–700 个汉字 / 700–1000 个英文单词。
> 阿里云百炼**新用户有免费额度（数千万 tokens）**，所以前几个月很可能**实际付费 ≈ 0**。

---

## 二、什么时候会花钱（哪些动作调用模型）

只有"调一次模型"才产生费用。分两类：

### A. 用户主动触发（点了 / 打开对话才算钱）
| 功能 | 接口 | 省钱设计 |
|---|---|---|
| 私信里外语消息自动翻成中文 | `/api/translate` | **24 小时缓存**，同一句不重复扣费 |
| 🧠 AI 理解（打开对话自动出阶段/下一步/风险） | `/api/analyze` | **有缓存，消息没变就不重算** |
| 生成双语回复 / 逐句对齐 | `/api/reply` `/api/align` | flash |
| 忠实翻译 / 润色 / AI 改写 | `/api/rewrite` | flash |
| 这是什么意思 / 这怎么办 | `/api/ask` | flash |
| 合作进度自动总结 | `/api/summary` | 增量更新，不重头算 |
| 智能加待办 / 催场话术 | `/api/parse-todo` `/api/followup` | 单次很小 |

### B. 后台自动（你不操作时）
| 功能 | 接口 | 省钱设计 |
|---|---|---|
| 提醒判断（该不该提醒、卡在哪步） | `/api/judge` | **只在某条对话有新进展时才跑一次**（靠签名去重 + 1 小时缓存），**不是每小时把所有对话都重烧一遍** |

### C. ⭐ 资源库 + 进度看板（那张表）= **完全不花 AI 的钱**
- 那两个管理端网页（红人资源库 / 进度看板）**一次模型都不调**。
- 它只是把各人插件**已经备份好的数据**读出来、按红人合并、做成可筛选的表格。
- 所以**"那张表"的 AI 成本 = ¥0**，只占后端一点点算力（和插件后端同一台服务器，不额外加钱）。

---

## 三、20 个人同时用，一个月大概多少钱

> 下面按"一个运营较忙的一天"估算，再乘 20 人 × 22 个工作日。tokens 数是按各功能实际调用规模 + 调用频次估的，已往高里算。

**单个运营 · 忙碌的一天**（自动翻译 + 开对话理解 + 写几十条回复 + 后台判断…）累计约 **40–60 万 tokens/天**，其中大部分是"读进去"的输入（便宜），少部分是"生成"的输出。

折算成钱：

| 强度 | 每人每天 | 每人每月（22 天） | **20 人每月** |
|---|---|---|---|
| 普通 | 约 ¥0.3–0.4 | 约 ¥7–9 | **约 ¥150–200** |
| 重度（再翻一倍） | 约 ¥0.6–0.8 | 约 ¥14–18 | **约 ¥300–400** |

**加上服务器：** 插件后端 + 资源库/看板**共用同一台 VPS**（一台就够），按常见规格约 **¥80–200/月**。

### 💰 合计（20 人）

| 项目 | 月成本 |
|---|---|
| AI 调用（qwen-flash） | 约 ¥150–400 |
| 服务器 VPS（一台，跑两个服务） | 约 ¥80–200 |
| **总计** | **约 ¥250–600 / 月** |
| 前期（吃百炼免费额度） | **可能接近 ¥0** |

> 即使把用量再估高一倍、把单价也按上限算，一个月也很难超过 ¥1000。这就是"全线用 flash + 大量缓存 + 那张表不调 AI"带来的结果。

---

## 四、它带来什么（产出 / 效益）

### 1. 直接省人力时间
- **不用手动翻译**：红人外语消息自动出中文，看懂秒级。
- **不用从零写回复**：双语回复一键生成、改到满意，复制就发。
- **不用翻聊天记录找进度**：合作进展后台自动整理成清单。
- **不漏回、不忘催**：提醒三桶 + 逐级催场，谁该回谁该催一目了然。

保守按**每人每天省 0.5–1 小时**算：
> 20 人 × 0.75 小时 × 22 天 ≈ **每月省下约 330 小时人力**。
> 哪怕只按实习生 ¥20–30/小时折算，也是 **¥6,600–10,000/月的人力价值** —— 是工具成本的 **十几到几十倍**。

### 2. 降低出错风险（这部分省的是"赔不起的钱"）
- **对外话术 / 内部建议严格隔离**：AI 把"能发给红人的话"和"只给运营看的内部建议、预算上限"分开，防止把内部信息、乱编的报价发出去。
- **不编造商务事实**：金额、授权期限、付款承诺没确认就不让 AI 填。
- **日语等敬语场景自动礼貌**：避免因语气失礼丢掉合作。
- **跟进升级**：该催就催、该体面退出就用委婉措辞，减少"鸽掉/谈崩"。

### 3. 管理层看得见（资源库 + 看板）
- 同一个红人在不同对接人手里的评价、合作几次、优质率、是否拉黑，**按 IG 号自动合并成一张表**，领导一眼判断"这个人值不值得再合作"。
- 进度看板看每个合作走到哪、谁卡住该催 —— **不用人工维护飞书表**，数据直接来自运营日常用的插件。

### 4. 新人快速上手
- 话术库 + AI 理解兜底，实习生不靠老带新也能稳住基本沟通质量。

---

## 五、一句话总结给领导

> **每月几百元的工具成本（前期可能为 0），换来每月数百小时人力释放、显著降低对外沟通出错、外加一套管理层随时能看的红人资源 / 进度看板。投入产出比在 10 倍以上，且用量越大单位成本越低（缓存 + flash）。**

---

> 附：真实开销随时可在**阿里云百炼控制台**按天查看；模型配置以后端 `/health` 为准。本文数字为基于代码实际配置 + 官方计费的估算，用于决策参考。
